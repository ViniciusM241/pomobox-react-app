import styled from 'styled-components';

export const Container = styled.div`
    padding: 2rem;
    box-shadow: 10 10 10 rgba(0, 0, 0, .5);

`;

export const Close = styled.h1`
    color: var(--color-primary);
`;